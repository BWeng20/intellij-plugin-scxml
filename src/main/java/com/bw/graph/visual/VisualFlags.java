package com.bw.graph.visual;

/**
 * Static interface with possible flags bits.
 */
public interface VisualFlags
{
	/**
	 * Draw always.
	 */
	int ALWAYS = 1;

	/**
	 * Draw if selected.
	 */
	int SELECTED = 2;
}
